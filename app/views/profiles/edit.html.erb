<h2>Edit Profile</h2>

<%= form_for @profile, :html => {:class => 'form-stacked'} do |f| %>
	<%= render :partial => '/shared/error_messages', :locals => {:model => @profile} %>
	<div class=field>
		<%= f.label :quote %><br>
		<%= f.text_area :quote, :class => 'xlarge', :rows => 5 %>
	</div>
	
	<%= f.submit "Save" %>
<% end %>
